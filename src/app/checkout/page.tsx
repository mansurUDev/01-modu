// Placeholder — real shipping form + order summary + guards land in T7.
export default function CheckoutPage() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center gap-4 bg-background px-6 text-center">
      <p className="font-mono text-mono-label uppercase tracking-[0.12em] text-muted">
        Checkout
      </p>
      <h1 className="font-display text-h2 text-heading">Almost there.</h1>
      <p className="max-w-md text-body">Shipping form comes in T7.</p>
    </main>
  );
}
