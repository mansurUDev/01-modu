// Placeholder — real success animation + sessionStorage guard land in T7.
export default function SuccessPage() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center gap-4 bg-background px-6 text-center">
      <p className="font-mono text-mono-label uppercase tracking-[0.12em] text-muted">
        Order
      </p>
      <h1 className="font-display text-h2 text-heading">Order sent.</h1>
      <p className="max-w-md text-body">Success animation comes in T7.</p>
    </main>
  );
}
